import Controller from '@ember/controller';

export default Ember.Controller.extend({
    // actions: {
    //   savenote(todos) {
    //     let self = this;
    //     todos.save().then(function() {
    //       self.transitionToRoute('todos');
    //     }, function(response){});
    //   },
    // }
  });
