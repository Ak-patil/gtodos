import Controller from '@ember/controller';

export default Controller.extend({
//   actions: {
//     filterByNote(param) {
//       if (param !== '') {
//         return this.store.query('todos', { userId: param });
//       } else {
//         return this.store.findAll('todos');
//       }
//     }
//   }
});